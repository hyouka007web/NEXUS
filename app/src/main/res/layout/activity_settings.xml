<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg_base">
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/topbar_height"
        android:gravity="center_vertical"
        android:paddingHorizontal="12dp"
        android:background="@color/bg_surface">
        <ImageButton
            android:id="@+id/settingsBackButton"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@drawable/ripple_accent_circle"
            android:src="@android:drawable/ic_menu_close_clear_cancel"
            android:contentDescription="Zurück"
            app:tint="@color/text_primary"
            xmlns:app="http://schemas.android.com/apk/res-auto" />
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text="NEXUS · Einstellungen"
            android:textColor="@color/text_primary"
            android:textSize="20sp" />
    </LinearLayout>
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="SUCHMASCHINE"
                android:textColor="@color/text_muted"
                android:textSize="12sp" />
            <RadioGroup
                android:id="@+id/searchEngineGroup"
                android:layout_width="match_parent"
                android:layout_height="wrap_content">
                <RadioButton android:id="@+id/engineDuckDuckGo" android:layout_width="match_parent" android:layout_height="48dp" android:text="DuckDuckGo" android:textColor="@color/text_primary" android:buttonTint="@color/accent_primary" />
                <RadioButton android:id="@+id/engineGoogle" android:layout_width="match_parent" android:layout_height="48dp" android:text="Google" android:textColor="@color/text_primary" android:buttonTint="@color/accent_primary" />
                <RadioButton android:id="@+id/engineBing" android:layout_width="match_parent" android:layout_height="48dp" android:text="Bing" android:textColor="@color/text_primary" android:buttonTint="@color/accent_primary" />
            </RadioGroup>
            <View android:layout_width="match_parent" android:layout_height="1dp" android:background="@color/border_hairline" android:layout_marginVertical="16dp" />
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:gravity="center_vertical">
                <TextView
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="Adblocker aktiv"
                    android:textColor="@color/text_primary"
                    android:textSize="15sp" />
                <Switch
                    android:id="@+id/adBlockSwitch"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:thumbTint="@color/accent_primary" />
            </LinearLayout>
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="32dp"
                android:text="NEXUS Browser · 0.2.0-alpha"
                android:textColor="@color/text_muted"
                android:textSize="12sp" />
        </LinearLayout>
    </ScrollView>
</LinearLayout>
