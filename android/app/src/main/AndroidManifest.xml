<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    android:name="io.flutter.app.FlutterApplication"
    android:label="nexus_flutter">

    <!-- Fix 1: INTERNET-Permission ist zwingend erforderlich für WebView und Dart:io HttpClient -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Fix 1: Netzwerk-Zustand für bessere Fehlerbehandlung -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Fix 1: Cache-Löschung und Cookie-Management -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:label="NEXUS"
        android:name="${applicationName}"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true"
        android:exported="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|layoutDirection|fontScale|screenLayout| density|viewLayout"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Fix 1: SplashScreen für Cold-Start-Verbesserung -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>

        <!-- Fix 4: Mediathek-Activity für PDF/EPUB Downloads -->
        <activity
            android:name=".MediathekActivity"
            android:exported="true"
            android:theme="@style/LaunchTheme">
            <intent-filter>
                <action android:name="com.nexus.browser.MEDIATHEK"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
    </application>
</manifest>
