name: Build APK

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-apk:
    name: Build Android APK
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Flutter SDK
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          flutter-version: '3.29.0'
          cache: true

      - name: Verify Flutter installation
        run: flutter --version

      - name: Create local.properties (CI has no flutter.local.properties)
        run: |
          echo "flutter.sdk=$$FLUTTER_ROOT" > android/local.properties
          echo "flutter.versionCode=1" >> android/local.properties
          echo "flutter.versionName=0.2.0" >> android/local.properties
          cat android/local.properties

      - name: Install dependencies
        run: flutter pub get

      - name: Analyze code (optional, non-blocking)
        run: flutter analyze || echo "Analysis completed with warnings"

      - name: Build APK (release, split per ABI)
        run: flutter build apk --release --split-per-abi --no-daemon

      - name: Collect APK artifacts
        uses: actions/upload-artifact@v4
        with:
          name: nexus-apk
          path: build/app/outputs/apk/release/
          retention-days: 14
          if-no-files-found: error
